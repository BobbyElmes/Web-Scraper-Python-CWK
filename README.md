# Web Scraper Python CWK
 A simple web scraper which crawls web pages until there are no more to crawl in a first in first out queue. Parses the pages to find the words and build an inverted index then allows for some searches to be done to query the inverted index.
